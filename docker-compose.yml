version: "3.8"

services:
  postgres-db:
    image: postgres:15.3
    restart: always
    environment:
      - POSTGRES_DB=tecnogamer_v2
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=123456
    volumes:
      - ./postgres:/var/lib/postgresql/data
    ports:
      - 5500:5432
